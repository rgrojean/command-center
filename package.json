{
  "name": "command-center",
  "version": "0.1.0",
  "private": true,
  "description": "Breaking Change Command Center — agentic PIS v3 migration pipeline on the Cursor SDK",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "pipeline": "tsx src/index.ts",
    "reset": "bash reset.sh",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.2",
    "zod-to-json-schema": "^3.24.5"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
